# ls

Purpose:
*Listing all contents of the current directory.*

Examples:

`ls`
Basic Listing all items

`ls -l`
Long Listing with permissions, owner, size and date

`ls -a`
Shows hidden items

`ls -la`
Shows hidden items with long listing format

My Notes:
*I mostly use ls for listing while navigating folders*