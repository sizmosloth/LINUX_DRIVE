# git remote

Purpose:
*Used to manage remote repositories.*

Examples:

`git remote -v`
Show connected repositories.

`git remote add origin (URL)`
Add a remote repository.

My Notes:
*Useful when connecting a local repository to GitHub.*