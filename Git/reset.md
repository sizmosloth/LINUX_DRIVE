# git reset

Purpose:
*Used to undo changes or move back to a previous commit.*

Examples:

`git reset HEAD~1`
Goes one commit back

`git reset --soft HEAD~1`
Goes one commit back but keep changes staged

`git reset --hard HEAD~1`
Goes one commit back and removes changes

My Notes:
*Useful when i made a mistake to go back*