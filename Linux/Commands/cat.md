# cat

Purpose:
*Used to display a file in txt format*

Examples:

`cat notes.txt`
Displays notes.txt file in txt format

`cat image.jpeg`
Displays image.jpeg file in txt format

My Notes:
*I use this only to display txt files only.*
