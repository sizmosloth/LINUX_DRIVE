# grep

Purpose:
*Used to search for texts inside files*

Examples:

`grep "hello" wakeup.txt`

Output:

`hello good morning`

**Use -i option for ignoring uppercase and lowercase**

My Notes:
*Useful for searching text but i don't use it at all =)*