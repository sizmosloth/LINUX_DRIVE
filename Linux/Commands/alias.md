# alias

Purpose:
*Used to create shortcuts for commands*

Examples:

`alias py='python3'`
Means python3 can be used as py

`alias root='cd /'
Means writting root in terminal will run cd / command and it will take you to root

View aliases:

`alias`

My Notes:
*I have made shortcut names for my project folders so i can just write name and i will go to the folder no need to do cd multiple times.*
