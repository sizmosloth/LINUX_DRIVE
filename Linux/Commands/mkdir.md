# mkdir

Purpose:
*Used to make folders inside a folder*

Examples:

`mkdir public`
Makes a folder with name public

`mkdir bin`
Makes a folder with name bin

My Notes:
*I Use this to make folders with cli.*